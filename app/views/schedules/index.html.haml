= render 'navbar', active: 'list_mode'
= render 'types_of_week_navbar'

.table-responsive
  %table.table
    %thead
      %tr
        %th= t('.day_of_week')
        %th= t('.lecture_number')
        %th= t('.subject')
        %th= t('.group')
        %th= t('.lecture_room')

    = render partial: 'day_of_week', collection: @days_of_week, locals: { type_of_week: @current_type }
= link_to t('.add_schedule'), new_schedule_path, class: 'btn btn-primary'